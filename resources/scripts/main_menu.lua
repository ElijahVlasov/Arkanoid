function option_click(event)
    --system.exit()
    system.show_menu("ui/pause_menu.xml")
end

function click(event)

    system.exit()

	--menu = system.get_main_menu()

	--b = system.ui.button()

	--if b == nil then
		--system.quit()
	--end

	--b.set_rect(200, 200, 200, 200)
	--b.text = "Text"

	--if(menu == nil)

	--	system.quit()

	--end

	--menu.add_component(b)

	--p = system.geometry.point(1.0, 1.0)
	--system.exit()

    print("test")
end
